#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Создание PDF документации по функционалу сайта мониторинга проектов
"""

from reportlab.lib.pagesizes import letter, A4
from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
from reportlab.lib.units import inch, cm
from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer, PageBreak, Image, Table, TableStyle, KeepTogether
from reportlab.lib.enums import TA_CENTER, TA_LEFT, TA_JUSTIFY
from reportlab.lib import colors
from datetime import datetime
import os

# Настройки документа
OUTPUT_FILE = 'Dokumentation_BuildViewHub.pdf'
PAGE_SIZE = A4
MARGIN = 1 * cm

# Создание PDF документа
doc = SimpleDocTemplate(
    OUTPUT_FILE,
    pagesize=PAGE_SIZE,
    rightMargin=MARGIN,
    leftMargin=MARGIN,
    topMargin=MARGIN,
    bottomMargin=MARGIN
)

# Стили
styles = getSampleStyleSheet()

# Кастомные стили
title_style = ParagraphStyle(
    'CustomTitle',
    parent=styles['Heading1'],
    fontSize=24,
    textColor=colors.HexColor('#1a365d'),
    spaceAfter=12,
    alignment=TA_CENTER,
    fontName='Helvetica-Bold'
)

heading_style = ParagraphStyle(
    'CustomHeading',
    parent=styles['Heading2'],
    fontSize=16,
    textColor=colors.HexColor('#2d5a8c'),
    spaceAfter=10,
    spaceBefore=10,
    fontName='Helvetica-Bold'
)

subheading_style = ParagraphStyle(
    'CustomSubHeading',
    parent=styles['Heading3'],
    fontSize=13,
    textColor=colors.HexColor('#3d7ab8'),
    spaceAfter=8,
    spaceBefore=8,
    fontName='Helvetica-Bold'
)

body_style = ParagraphStyle(
    'CustomBody',
    parent=styles['BodyText'],
    fontSize=11,
    alignment=TA_JUSTIFY,
    spaceAfter=8,
    leading=14
)

# Контент документа
content = []

# ===== ТИТУЛЬНАЯ СТРАНИЦА =====
content.append(Spacer(1, 2*cm))
content.append(Paragraph(
    "🏗️ МОНИТОРИНГ ПРОЕКТОВ",
    title_style
))
content.append(Spacer(1, 0.5*cm))
content.append(Paragraph(
    "Система интеллектуальной оценки рисков государственного жилищного строительства",
    ParagraphStyle('subtitle', parent=styles['Normal'], fontSize=13, alignment=TA_CENTER, textColor=colors.HexColor('#555555'))
))
content.append(Spacer(1, 3*cm))

# Информация о документе
doc_info_data = [
    ['Версия документа:', 'v1.0'],
    ['Дата создания:', datetime.now().strftime('%d.%m.%Y')],
    ['Язык:', 'Русский / English'],
    ['Платформа:', 'Астро + React + TypeScript']
]
doc_info_table = Table(doc_info_data, colWidths=[4*cm, 8*cm])
doc_info_table.setStyle(TableStyle([
    ('BACKGROUND', (0, 0), (0, -1), colors.HexColor('#f0f0f0')),
    ('TEXTCOLOR', (0, 0), (-1, -1), colors.black),
    ('ALIGN', (0, 0), (-1, -1), 'LEFT'),
    ('FONTNAME', (0, 0), (0, -1), 'Helvetica-Bold'),
    ('FONTSIZE', (0, 0), (-1, -1), 11),
    ('BOTTOMPADDING', (0, 0), (-1, -1), 10),
    ('TOPPADDING', (0, 0), (-1, -1), 10),
    ('GRID', (0, 0), (-1, -1), 0.5, colors.grey)
]))
content.append(doc_info_table)
content.append(Spacer(1, 3*cm))

content.append(PageBreak())

# ===== ОГЛАВЛЕНИЕ =====
content.append(Paragraph("📑 ОГЛАВЛЕНИЕ", heading_style))
content.append(Spacer(1, 0.3*cm))

toc_items = [
    "1. Обзор системы",
    "2. Главная страница (Home)",
    "3. Дашборд проектов (Dashboard)",
    "4. Список проектов (Projects)",
    "5. Загрузка отчётов (Upload Reports)",
    "6. Детали проекта (Project Details)",
    "7. Система классификации статусов",
    "8. Алгоритмы расчёта метрик",
    "9. Ручные критерии и флаги",
    "10. Техническая архитектура"
]

for item in toc_items:
    content.append(Paragraph(item, body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 1: ОБЗОР СИСТЕМЫ =====
content.append(Paragraph("1️⃣ Обзор системы", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Система мониторинга проектов предназначена для комплексной оценки рисков государственного жилищного строительства. "
    "Платформа анализирует финансовые отчёты (PDF), автоматически извлекает ключевые метрики и классифицирует статус проекта "
    "на основе многоуровневых критериев.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Основные возможности:</b>", body_style))
content.append(Spacer(1, 0.1*cm))

features = [
    "✅ Загрузка и анализ PDF отчётов с автоматическим извлечением данных",
    "✅ Классификация проектов по 4 уровням статуса (Критичный/Тревожный/Нормальный/Информационный)",
    "✅ Вычисление вероятности рисков на основе ML-алгоритмов",
    "✅ Хранение полной истории отчётов и изменения статусов",
    "✅ Поддержка ручного редактирования критериев по условиям D",
    "✅ Пакетную обработку и дедупликацию отчётов по месяцам",
    "✅ Многоязычный интерфейс (Русский/Английский)"
]

for feature in features:
    content.append(Paragraph(feature, body_style))
    content.append(Spacer(1, 0.15*cm))

content.append(Spacer(1, 0.5*cm))
content.append(PageBreak())

# ===== РАЗДЕЛ 2: ГЛАВНАЯ СТРАНИЦА =====
content.append(Paragraph("2️⃣ Главная страница (Home)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Главная страница служит точкой входа в систему. Она предоставляет краткий обзор определение ключевых показателей "
    "мониторинга и быстрый доступ к основным разделам приложения.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Элементы главной страницы:</b>", body_style))
content.append(Spacer(1, 0.1*cm))

home_elements = [
    ("🎯 Навигационное меню", "Основное меню с быстрым доступом к 'Дашборду', 'Проектам' и 'Загрузке отчётов'"),
    ("📊 Статистика", "Итоговые показатели количества проектов, активных мониторов и последних событий"),
    ("🚨 Критические проекты", "Список проектов со статусом 'Критичный' требующих немедленного внимания"),
    ("⚠️ Тревожные проекты", "Проекты требующие повышенного внимания и профилактических мер"),
    ("📚 FAQ & Документация", "Ссылки на руководства и документацию для новых пользователей")
]

for title, desc in home_elements:
    content.append(Paragraph(f"<b>{title}</b>: {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("[СКРИНШОТ: Главная страница]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 3: ДАШБОРД =====
content.append(Paragraph("3️⃣ Дашборд проектов (Dashboard)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Дашборд предоставляет интерактивный обзор всех проектов с возможностью фильтрации, сортировки и быстрого доступа к детальной информации.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Ключевые компоненты дашборда:</b>", body_style))
content.append(Spacer(1, 0.1*cm))

dashboard_features = [
    ("📈 Карточки проектов", "Каждый проект отображается в виде компактной карточки с основными метриками и цветовый статус-индикатор"),
    ("🔍 Фильтры", "Фильтрация по статусу, риск-уровню, дате последнего обновления и другим критериям"),
    ("⬆️⬇️ Сортировка", "Сортировка по названию, статусу, проценту готовности, отставанию по срокам"),
    ("🔗 Быстрые ссылки", "Прямой переход к загрузке отчётов или просмотру деталей каждого проекта"),
    ("🔄 Автоматическое обновление", "Данные обновляются в реальном времени при загрузке новых отчётов")
]

for title, desc in dashboard_features:
    content.append(Paragraph(f"<b>{title}</b>: {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("[СКРИНШОТ: Дашборд с проектами]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 4: СПИСОК ПРОЕКТОВ =====
content.append(Paragraph("4️⃣ Список проектов (Projects)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Расширенный список всех проектов с подробной информацией по каждому. Позволяет массовое управление и анализ данных.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Функционал страницы Projects:</b>", body_style))
content.append(Spacer(1, 0.1*cm))

projects_features = [
    ("📋 Табличный вид", "Полный список проектов с колонками: название, код, статус, вероятность риска, СМР%, отставание"),
    ("🏷️ Код проекта", "Уникальный идентификатор каждого проекта (например: 'Сертификат №182, ДПГ-23-13-055/132')"),
    ("🎯 Статусы", "Визуальные индикаторы статуса (🟢 Нормальный, 🟡 Тревожный, 🔴 Критичный)"),
    ("⚡ ML Risk Score", "Вероятность риска рассчитанная машинным обучением (0-100%)"),
    ("🔧 Действия", "Возможности редактирования, удаления и перейти к детальной информации")
]

for title, desc in projects_features:
    content.append(Paragraph(f"<b>{title}</b>: {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("[СКРИНШОТ: Список проектов в таблице]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 5: ЗАГРУЗКА ОТЧЁТОВ =====
content.append(Paragraph("5️⃣ Загрузка отчётов (Upload Reports)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Раздел для загрузки PDF отчётов и их анализа. Система автоматически извлекает ключевые метрики и создаёт запись в базе данных.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Процесс загрузки и анализа:</b>", body_style))
content.append(Spacer(1, 0.2*cm))

upload_steps = [
    ("1️⃣ Выбор файла", "Пользователь выбирает PDF файл через интерфейс (drag-drop или file picker)"),
    ("2️⃣ Отправка на анализ", "Файл отправляется на Python backend для анализа (timeout: 45 сек)"),
    ("3️⃣ Извлечение данных", "Backend использует pdfplumber для парсинга таблиц и извлечения метрик: СМР%, ГПР%, ДДУ_monthly"),
    ("4️⃣ Классификация", "Система классифицирует статус проекта по 4 условиям (A, B, C, D)"),
    ("5️⃣ Ручной ввод (если нужен)", "Если код проекта не распознан, пользователь вводит название вручную"),
    ("6️⃣ Сохранение", "Проект и отчёт сохраняются в БД Wix, PDF добавляется в историю")
]

for step, desc in upload_steps:
    content.append(Paragraph(f"<b>{step}</b> - {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("<b>Извлекаемые метрики:</b>", body_style))
content.append(Spacer(1, 0.1*cm))

metrics = [
    "🏗️ СМР (Строительно-монтажные работы) - % готовности",
    "💰 ГПР (График поступления денег) - задержка в % и днях",
    "🏦 ДДУ (Договор долевого участия) - месячные платежи (М1, М2, М3)"
]

for metric in metrics:
    content.append(Paragraph(metric, body_style))
    content.append(Spacer(1, 0.15*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("[СКРИНШОТ: Форма загрузки и результаты анализа]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 6: ДЕТАЛИ ПРОЕКТА =====
content.append(Paragraph("6️⃣ Детали проекта (Project Details)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Страница показывает полную информацию о проекте: все метрики, историю изменений, загруженные PDF и рекомендации.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Разделы страницы Project Details:</b>", body_style))
content.append(Spacer(1, 0.2*cm))

detail_sections = [
    ("📌 Информация о проекте", "Название, код, расположение, заказчик"),
    ("📊 Ключевые метрики", "СМР%, отставание ГПР%, платежи по ДДУ, вероятность риска ML"),
    ("🎨 Оценка рисков", "Текущий статус, анализ условий A/B/C/D, рекомендации"),
    ("📈 История отчётов (3 месяца)", "Таблица с последними 3 отчётами со статусами и метриками"),
    ("📄 Загруженные PDF", "Таблица со всеми загруженными PDF с датами и возможностью скачивания"),
    ("🚩 Флаги и предупреждения", "Критические события и ручные флаги (D1-D4)"),
    ("📜 История изменения статуса", "Логирование всех изменений статуса проекта со временем")
]

for section, desc in detail_sections:
    content.append(Paragraph(f"<b>{section}</b>: {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.3*cm))
content.append(Paragraph("[СКРИНШОТ: Страница детали проекта]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 7: СИСТЕМА КЛАССИФИКАЦИИ =====
content.append(Paragraph("7️⃣ Система классификации статусов", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Проект классифицируется в одну из 4 категорий на основе 4 условий (A, B, C, D). При выполнении всех условий статус переходит в более высокий уровень риска.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

# Таблица статусов
status_data = [
    ['Статус', 'Цвет', 'Описание', 'Действия'],
    ['🟢 Нормальный', 'Зелёный', 'Все условия в норме', 'Мониторинг'],
    ['🟡 Тревожный', 'Жёлтый', 'Частичное нарушение условий', 'Внимание и контроль'],
    ['🔴 Критичный', 'Красный', 'Серьёзные нарушения всех/большинства условий', 'Немедленные меры'],
    ['⚪ Информационный', 'Серый', 'Недостаточно данных для оценки', 'Загрузить доп. отчёты']
]

status_table = Table(status_data, colWidths=[2.5*cm, 2*cm, 4*cm, 4*cm])
status_table.setStyle(TableStyle([
    ('BACKGROUND', (0, 0), (-1, 0), colors.HexColor('#2d5a8c')),
    ('TEXTCOLOR', (0, 0), (-1, 0), colors.whitesmoke),
    ('ALIGN', (0, 0), (-1, -1), 'LEFT'),
    ('FONTNAME', (0, 0), (-1, 0), 'Helvetica-Bold'),
    ('FONTSIZE', (0, 0), (-1, 0), 10),
    ('BOTTOMPADDING', (0, 0), (-1, 0), 12),
    ('TOPPADDING', (0, 0), (-1, 0), 12),
    ('BACKGROUND', (0, 1), (-1, -1), colors.HexColor('#f9f9f9')),
    ('GRID', (0, 0), (-1, -1), 0.5, colors.grey),
    ('ROWBACKGROUNDS', (0, 1), (-1, -1), [colors.white, colors.HexColor('#f0f0f0')])
]))
content.append(status_table)
content.append(Spacer(1, 0.5*cm))

content.append(Paragraph("<b>Условия классификации:</b>", body_style))
content.append(Spacer(1, 0.2*cm))

conditions = [
    ("A: СМР < 80%", "Если готовность строительно-монтажных работ менее 80% → КРИТИЧНЫЙ"),
    ("B: ДДУ недостаток", "Если накопленные платежи ДДУ < 70% от ГПР → ТРЕВОЖНЫЙ; <50% → КРИТИЧНЫЙ"),
    ("C: Отставание ГПР > 30%", "Если задержка денег > 30% от нормативного срока → КРИТИЧНЫЙ"),
    ("D: Ручные флаги", "Гарантийные случаи, жалобы, снижение рейтинга, просрочки → КРИТИЧНЫЙ")
]

for cond, desc in conditions:
    content.append(Paragraph(f"<b>Условие {cond}</b>: {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.5*cm))
content.append(PageBreak())

# ===== РАЗДЕЛ 8: АЛГОРИТМЫ РАСЧЁТА =====
content.append(Paragraph("8️⃣ Алгоритмы расчёта метрик", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Система использует математические формулы для расчёта ключевых показателей. Все вычисления задокументированы для обеспечения прозрачности и воспроизводимости.",
    body_style
))
content.append(Spacer(1, 0.5*cm))

content.append(Paragraph("<b>1. Рассчёт B1 (Отставание ГПР):</b>", subheading_style))
content.append(Paragraph(
    "Отставание (%) = (Дни отставания / Нормативный срок строительства) × 100<br/>"
    "<br/>"
    "Пример: Если проект должен завершиться в 570 дней, а уже прошло 450 дней отставания:<br/>"
    "450 / 570 × 100 = <b>78.9%</b><br/>"
    "<br/>"
    "Если результат > 30% → условие B1 ВЫПОЛНЕНО (ТРЕВОЖНО)",
    body_style
))
content.append(Spacer(1, 0.4*cm))

content.append(Paragraph("<b>2. Рассчёт B6 (Накопленные ДДУ):</b>", subheading_style))
content.append(Paragraph(
    "Система проверяет комулятивный процент поступлений по ДДУ за 3 месяца:<br/>"
    "• Месяц 1: М1 / ГПР < 70% → условие выполнено<br/>"
    "• Месяцы 1+2: (М1+М2) / ГПР < 60% → условие выполнено<br/>"
    "• Месяцы 1+2+3: (М1+М2+М3) / ГПР < 50% → КРИТИЧНЫЙ<br/>"
    "<br/>"
    "<b>Примечание:</b> Значения М1, М2, М3 автоматически конвертируются из тенге в млн тенге (÷1,000,000)",
    body_style
))
content.append(Spacer(1, 0.4*cm))

content.append(Paragraph("<b>3. ML Risk Probability:</b>", subheading_style))
content.append(Paragraph(
    "Вероятность риска рассчитывается на основе взвешенной комбинации всех условий:<br/>"
    "<br/>"
    "Risk = (условия A, B, C × веса коэффициента) + (D флаги × критический вес)<br/>"
    "<br/>"
    "Результат: число от 0-100% где 0% = нет риска, 100% = максимальный риск",
    body_style
))
content.append(Spacer(1, 0.5*cm))

content.append(Paragraph("[СКРИНШОТ: Формулы на странице проекта]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 9: РУЧНЫЕ КРИТЕРИИ =====
content.append(Paragraph("9️⃣ Ручные критерии и флаги (Условие D)", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Если автоматические данные неполные или нужно учесть критические события, пользователь может вручную установить флаги D1-D4.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

# Таблица D флагов
d_flags_data = [
    ['Флаг', 'Описание', 'Статус'],
    ['D1: Гарантия', 'Продление гарантии / гарантийный случай', 'КРИТИЧНЫЙ'],
    ['D2: Жалобы', 'Более 1 жалобы дольщиков', 'КРИТИЧНЫЙ'],
    ['D3: Рейтинг', 'Снижение рейтинга на 20+ баллов', 'КРИТИЧНЫЙ'],
    ['D4: Займы', 'Просрочка по займам/кредитам', 'КРИТИЧНЫЙ']
]

d_flags_table = Table(d_flags_data, colWidths=[2*cm, 6*cm, 3*cm])
d_flags_table.setStyle(TableStyle([
    ('BACKGROUND', (0, 0), (-1, 0), colors.HexColor('#c41e3a')),
    ('TEXTCOLOR', (0, 0), (-1, 0), colors.whitesmoke),
    ('ALIGN', (0, 0), (-1, -1), 'LEFT'),
    ('FONTNAME', (0, 0), (-1, 0), 'Helvetica-Bold'),
    ('FONTSIZE', (0, 0), (-1, 0), 10),
    ('BOTTOMPADDING', (0, 0), (-1, 0), 10),
    ('TOPPADDING', (0, 0), (-1, 0), 10),
    ('BACKGROUND', (0, 1), (-1, -1), colors.HexColor('#ffe0e0')),
    ('GRID', (0, 0), (-1, -1), 0.5, colors.grey)
]))
content.append(d_flags_table)
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "<b>Как использовать:</b> На странице Project Details найти раздел 'Ручные флаги (d1–d4)'. "
    "Отметить нужные флаги и нажать 'Применить и пересчитать' для обновления статуса проекта.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("[СКРИНШОТ: Форма для установки ручных флагов D1-D4]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== РАЗДЕЛ 10: ТЕХНИЧЕСКАЯ АРХИТЕКТУРА =====
content.append(Paragraph("🔟 Техническая архитектура", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "System построена на современном стеке технологий с разделением на frontend и backend для обеспечения масштабируемости и надёжности.",
    body_style
))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Frontend стек:</b>", subheading_style))
frontend_tech = [
    "• Астро (Astro) - meta-framework для быстрой статической генерации страниц",
    "• React 18 - компонентный фреймворк для интерактивности",
    "• TypeScript - типизированный JavaScript для надежности",
    "• Tailwind CSS - утилита-first фреймворк для стилей",
    "• Radix UI - доступные компоненты UI"
]
for tech in frontend_tech:
    content.append(Paragraph(tech, body_style))
    content.append(Spacer(1, 0.1*cm))

content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Backend стек:</b>", subheading_style))
backend_tech = [
    "• Python Flask - лёгкий web-фреймворк для API",
    "• pdfplumber - парсинг таблиц и текста из PDF",
    "• Wix Data - облачная база данных для хранения проектов и отчётов",
    "• Port 5002 - основной порт для API"
]
for tech in backend_tech:
    content.append(Paragraph(tech, body_style))
    content.append(Spacer(1, 0.1*cm))

content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Data Flow (процесс передачи данных):</b>", subheading_style))
data_flow = [
    ("1. Upload", "Пользователь выбирает PDF → отправляет на backend →  /api/analyze-report"),
    ("2. Analysis", "Backend парсит PDF → извлекает метрики → классифицирует статус"),
    ("3. Response", "Backend возвращает analysisResult с метриками, статусом, условиями")  ,
    ("4. Save", "Frontend сохраняет проект в Wix DB (constructionprojects + projectreports)"),
    ("5. Display", "Frontend отображает результаты на ProjectDetailPage с историей и PDF"),
    ("6. Sync", "При следующей загрузке - дедупликация по месяцам, обновление метрик")
]
for step, desc in data_flow:
    content.append(Paragraph(f"<b>{step}:</b> {desc}", body_style))
    content.append(Spacer(1, 0.2*cm))

content.append(Spacer(1, 0.5*cm))
content.append(Paragraph("[ДИАГРАММА: Архитектура системы (Frontend → API → Backend → Database)]", ParagraphStyle('placeholder', parent=styles['Normal'], fontSize=10, textColor=colors.HexColor('#999999'), italic=True)))
content.append(Spacer(1, 2*cm))

content.append(PageBreak())

# ===== ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ =====
content.append(Paragraph("📌 Дополнительная информация", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Поддерживаемые языки:</b>", body_style))
content.append(Spacer(1, 0.1*cm))
content.append(Paragraph("🇷🇺 Русский (основной язык интерфейса)", body_style))
content.append(Paragraph("🇬🇧 English (планируется)", body_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Поддерживаемые форматы PDF:</b>", body_style))
content.append(Spacer(1, 0.1*cm))
content.append(Paragraph("✅ PDF с таблицами (рекомендуется)", body_style))
content.append(Paragraph("✅ PDF со сканированным текстом (OCR-обработка)", body_style))
content.append(Paragraph("⚠️ Максимальный размер файла: 50 MB", body_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph("<b>Ограничения и особенности:</b>", body_style))
content.append(Spacer(1, 0.1*cm))
content.append(Paragraph("• Анализ одного PDF занимает 10-45 секунд в зависимости от сложности", body_style))
content.append(Paragraph("• История отчётов хранится за 12 месяцев", body_style))
content.append(Paragraph("• До 12 PDF файлов может быть привязано к одному проекту", body_style))
content.append(Paragraph("• Дедупликация происходит на уровне месяца (один отчёт на месяц)", body_style))
content.append(Spacer(1, 0.5*cm))

# Заключение
content.append(PageBreak())
content.append(Paragraph("✅ Заключение", heading_style))
content.append(Spacer(1, 0.3*cm))

content.append(Paragraph(
    "Система мониторинга проектов BuildViewHub предоставляет комплексное решение для оценки рисков государственного жилищного строительства. "
    "Платформа объединяет автоматический анализ данных с возможностью ручного редактирования, обеспечивая точность и гибкость в управлении проектами.<br/>"
    "<br/>"
    "Благодаря интуитивному интерфейсу и прозрачным алгоритмам, система позволяет пользователям быстро идентифицировать критические проекты и принимать "
    "обоснованные решения на основе данных.<br/>"
    "<br/>"
    "<b>Желаем успешного использования системы!</b>",
    body_style
))

content.append(Spacer(1, 1*cm))

# Footer
content.append(Paragraph(
    f"Документация создана: {datetime.now().strftime('%d.%m.%Y %H:%M')}<br/>Версия: 1.0<br/>© 2026 BuildViewHub",
    ParagraphStyle('footer', parent=styles['Normal'], fontSize=9, alignment=TA_CENTER, textColor=colors.grey)
))

# Построение PDF
doc.build(content)
print(f"✅ PDF документ успешно создан: <b>{OUTPUT_FILE}</b>")
print(f"\n📊 Содержание документа:")
print("  1. Обзор системы")
print("  2. Главная страница")
print("  3. Дашборд проектов")
print("  4. Список проектов")
print("  5. Загрузка отчётов")
print("  6. Детали проекта")
print("  7. Система классификации")
print("  8. Алгоритмы расчёта")
print("  9. Ручные критерии (D1-D4)")
print("  10. Техническая архитектура")
print("\n🖼️  Все скриншоты отмечены placeholders для вставки")
